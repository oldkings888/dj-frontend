var sub = function (val) {
  if (val == undefined ||val.length == 0 ) {
    return;
  }
  if (val.length > 17) {
    return val.substring(0, 10);
  } else {
    return val;
  }
}
module.exports.sub = sub;